import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  article: {
    marginTop: 10,
    marginBottom: 10,
  },
  articleTitle: {
    // text-shadow: -1px 1px 10px rgba(0, 0, 0, 0.75)
    // textShadowColor: 'red',
    // textShadowOffset: { width: 2, height: 2 },
    // textShadowRadius : 5
    // boxShadow: "inset 0 -0.4em 0 0 rgba(56,58,167,.1)",
  }
})

